# Enterprise Analytics Agent (intentionally incomplete)

This bundled sample simulates a browser chat assistant that queries SQLite and a local mock CRM. It is deliberately unsafe and incomplete so Production Lens can demonstrate deterministic findings.

Do not use this sample as a production reference implementation.
